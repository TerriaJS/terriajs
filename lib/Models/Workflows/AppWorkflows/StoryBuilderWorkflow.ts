import AppWorkflow from "./AppWorkflow";

/**
 * Dummy workflow to handle StoryBuilder rendering
 */
export const StoryBuilderWorkflow: AppWorkflow = Object.freeze({
  name: "storyBuilderWorkflow"
});
