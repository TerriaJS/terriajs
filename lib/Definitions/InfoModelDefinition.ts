import ModelDefinition from './ModelDefinition';

const def = new ModelDefinition({
    type: 'info',
    name: 'Catalog Member Information',
    description: 'Free-form, textual information about a catalog member.'
});

const InfoModelDefinition = def;
export default InfoModelDefinition;
